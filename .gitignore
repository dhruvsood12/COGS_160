# macOS
.DS_Store
.AppleDouble
.LSOverride

# Claude Code local settings
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/

# Environment / credentials
.env
*.env

# Zip archives (submit individually, don't bloat git)
*.zip

# Editor
.idea/
.vscode/
*.swp

# Track 2 — external nested git repos (the cloned/forked Article Finder, Knowledge Atlas,
# atlas_shared). Each has its own .git and is committed/pushed separately to its own
# remote. Do not track them inside this parent repo. See track2/README.md.
track2/Article_Finder/
track2/Knowledge_Atlas/
track2/atlas_shared/
